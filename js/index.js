   $(document).ready(function() {
	  
	      $('.nav-link').css('visibility','visible').hide().fadeIn("3000");

	  /* $(document).on('scroll', function (){
		   var curr = $(this).scrollTop();
		   
		   $('.container,.container-fluid').each(function(){
			   if(curr > $(this).offset().top) $(this).css('opacity', 1 - (curr- $(this).offset().top)/400);
		   });
	   }); */
	   
   });